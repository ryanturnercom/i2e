# Example uses default effort tiers from i2e_core.config.
# Override here if you want a tighter budget — e.g. fail fast on adversarial constraints.

defaults:
  case_effort: medium
  target_effort: low
  watcher: '@me'

scheduler:
  cadence: weekly
  via: manual
